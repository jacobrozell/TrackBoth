import Foundation

// MARK: - Theme Preferences
/// Single source of truth for theme-related UserDefaults keys.
enum ThemePreferences {
    static let selectedTheme = "selectedTheme"
    static let selectedAppTheme = "selectedAppTheme"
    static let selectedFontDesign = "selectedFontDesign"
    static let hasCompletedOnboarding = "hasCompletedOnboarding"
}
